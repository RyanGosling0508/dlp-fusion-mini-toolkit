# DLP-Fusion Mini-Toolkit

A toolkit for translating DHL subset of Description Logic (DL) to Datalog and answering DL-style queries with an LP engine using SQLite.

## Week 1 Goals
- [ ] Finalize DHL subset & JSON schema
- [ ] Design SQLite schema  
- [ ] Create CLI stub

## Setup
```bash
conda activate dlp_fusion
pip install -r requirements.txt
```